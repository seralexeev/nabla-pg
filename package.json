{
  "name": "flstk",
  "version": "0.0.1",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "git clean -fdx && yarn",
    "compile": "yarn workspaces foreach run compile"
  },
  "devDependencies": {
    "ts-node": "9.1.1",
    "tsconfig-paths": "3.9.0",
    "typescript": "4.2.4"
  },
  "dependencies": {
    "ts-node-dev": "1.1.6"
  }
}
