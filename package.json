{
  "name": "flstk",
  "version": "0.0.1",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "git clean -fdx && yarn",
    "compile": "yarn workspaces foreach run compile"
  },
  "devDependencies": {
    "ts-node-dev": "1.1.6",
    "ttypescript": "1.5.12"
  }
}
