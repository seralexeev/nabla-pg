type Query {
  allMigrations(id: String!): [MigrationVm!]!
}

type MigrationVm {
  name: String!
  migratedAt: DateTime!
}

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime
